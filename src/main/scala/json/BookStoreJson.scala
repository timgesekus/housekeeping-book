package net
package gesekus
package housekeeping
package bookstorejson
import net.gesekus.housekeeping.algebra.book.Books

import argonaut._
import net.gesekus.housekeeping.algebra.book.Book
import net.gesekus.housekeeping.algebra.book.Book._


object BookStoreJson {
    /*
    def fromJson (bookStoreJson: String) : List[Book] = {
        bookStoreJson.decodeOption[List[Book]].getOrElse(Nil)
    }
    def toJson (books: List[Book]) : String = books.asJson.spaces2   
     */
}
